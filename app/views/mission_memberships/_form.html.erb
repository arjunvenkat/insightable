<%= form_for(@mission_membership, :html => { :class => "form-horizontal", :role => "form" }) do |f| %>
  <% if @mission_membership.errors.any? %>
    <div class="alert alert-danger alert-dismissable" role="alert">
      <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
      <h4><%= pluralize(@mission_membership.errors.count, "error") %> prohibited this mission_membership from being saved:</h4>

      <ul>
      <% @mission_membership.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :user_id, :class => "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.collection_select :user_id, User.all, :id, :name, :class => "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :mission_id, :class => "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.collection_select :mission_id, Mission.all, :id, :name, :class => "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <%= f.submit :class => "btn btn-primary" %>
    </div>
  </div>
<% end %>
